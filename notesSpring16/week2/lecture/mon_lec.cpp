/*
Integer is stored in 32bit, for whole numbers
Float is 32bit, for numbers with decimal
Double is 64 bit, twice the precision as float
Three parts to float/double. Sign (+/-), exp(2^x), and manitisa for significant precision.
*/

#include <iostream>
#include <cmath>

using namespace std;

int main(){
  cout <<sqrt(4)<<endl;
  return 0;
}
